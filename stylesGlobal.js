// Declaração de Variáveis de Paletas e etc
const colors = {
    primaryColor: '#7C37F4',
    bgColor: '#dbdbdb',
    dImgColor: '#d9c3ff',
    alertColor: '#d32626',
    seilairmaoColor: '#A11D86',
};

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: colors.bgColor,
        alignItems: 'center',
        justifyContent: 'center',
    },
});

export default styles;